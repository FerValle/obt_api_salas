Crear una reserva de butaca
---
tags:
  - Reservas
parameters:
  - name: body
    in: body
    required: true
    schema:
      type: object
      required:
        - idFuncion
        - idButaca
        - dni
      properties:
        idFuncion:
          type: integer
          description: ID de la función
          example: 12
        idButaca:
          type: integer
          description: ID de la butaca
          example: 13
        dni:
          type: string
          description: DNI del cliente (formato XX.XXX.XXX)
          example: "12.345.678"
responses:
  201:
    description: Reserva creada exitosamente
    schema:
      type: object
      properties:
        mensaje:
          type: string
          example: "Reserva creada exitosamente"
  400:
    description: Parámetros faltantes o inválidos
    schema:
      type: object
      properties:
        error:
          type: string
  404:
    description: Función o butaca no encontrada
    schema:
      type: object
      properties:
        error:
          type: string
  409:
    description: Conflicto - DNI con 4 reservas activas o butaca ya reservada
    schema:
      type: object
      properties:
        error:
          type: string
          example: "El DNI ya tiene 4 reservas activas para esa fecha"
  500:
    description: Error interno del servidor
